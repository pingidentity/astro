image: node:8

stages:
  - test

test:
  stage: test
  tags:
    - platform-build-node-fragment
  script:
    - npm i lerna -g
    - lerna bootstrap
    - lerna run lint
    - lerna run coverage-ci
    - lerna run static
    - lerna run pack
