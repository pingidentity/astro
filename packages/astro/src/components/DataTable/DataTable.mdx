<Meta title="Components/DataTable/DataTable" />

# DataTable

Inspired by [TableView](https://react-spectrum.adobe.com/react-spectrum/TableView.html)
from React Spectrum and [useTableState](https://react-spectrum.adobe.com/react-stately/useTableState.html)
from React Stately.

DataTable is a complex collection component that is built from many child elements including
columns, rows and cells. Columns are defined within DataTableHeader element via DataTableColumn
and rows are defined within a DataTableBody element via DataTableRow. Rows contain DataTableCell
elements that correspond to each column.

[TableHeader](https://react-spectrum.adobe.com/react-aria/useTable.html#:~:text=defined%20using%20the-,TableHeader,-%2C%20Column%2C)
uses the alias DataTableHeader.

[Column](https://react-spectrum.adobe.com/react-aria/useTable.html#:~:text=the%20TableHeader%2C-,Column,-%2C%20TableBody%2C)
uses the alias DataTableColumn.

[Cell](https://react-spectrum.adobe.com/react-aria/useTable.html#:~:text=Row%2C%20and-,Cell,-components%2C%20which%20support)
uses the alias DataTableCell.

[Row](https://react-spectrum.adobe.com/react-aria/useTable.html#:~:text=%2C%20TableBody%2C-,Row,-%2C%20and%20Cell%20components)
uses the alias DataTableCell.

[TableBody](https://react-spectrum.adobe.com/react-aria/useTable.html#:~:text=%2C%20Column%2C-,TableBody,-%2C%20Row%2C%20and)
uses the alias DataTableBody.


### Required Components

- DataTableHeader
- DataTableColumn
- DataTableBody
- DataTableRow
- DataTableCell

### Best Practices

DataTable should:
- have vertically aligned content.
- use multiple heading rows for higher-level grouping of the columns.
- sparingly use column dividers.

DataTable shouldn’t:
- use different indicators to represent empty fields.

### Accessibility

#### Keyboard Navigation

- DataTable is focusable using the Tab key and all focusable components follow the page Tab sequence.
- Shift + Tab moves focus to the previous focusable component.
- Components within are accessible via arrow keys and pressing Space/Enter when focused, triggers an action associated with the interactive component.
- Scrolling down after focusing on a row shifts the focus back to the entire table, instead of the previously focused row.
- Pressing Home(Fn + Right Arrow Key) or PageUp(Fn + Up Key) or Control/Command + Home shifts the focus to the first row.
- Pressing End(Fn + Left Arrow Key) or PageDown(Fn + Down Key) or Control/Command + End shifts the focus to the last visible row.

#### Screen Readers

DataTable utilizes **`aria-label** to give an accessible name to the component. 
It has an **`aria-rowcount`** and **`aria-columncount`** to indicate the total number of rows and columns in the grid structure. 
DataTableHeader uses **`aria-rowindex`** and DataTableColumn uses **`aria-colindex`** to enumerate the header row and columns. 
In the body element, DataTableRow has **`aria-labelledby`** supplied with the grid Id and **`aria-rowindex`** passed to each row. 
Each DataTableCell has **`aria-colindex`** to enumerate the columns within each row in the body.

| Aria Attribute | Role | Component |
| ---- | ---- | ----------- |
| `aria-label` | grid | DataTable |
| `aria-rowcount` | grid | DataTable |
| `aria-columncount` | grid | DataTable |
| `aria-rowindex` | row | DataTableHeader |
| `aria-colindex` | columnheader | DataTableColumn |
| `aria-labelledby` | row | DataTableRow |
| `aria-rowindex` | row | DataTableRow |
| `aria-colindex` | rowheader | DataTableCell |
| `aria-colindex` | gridcell | DataTableCell |