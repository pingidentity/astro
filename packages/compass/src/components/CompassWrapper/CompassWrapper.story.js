import React from 'react';
import { CompassWrapper } from '../../.';

export default {
    title: 'CompassWrapper',
    component: CompassWrapper,
};

export const Default = () => (
    <CompassWrapper>Content in a wrapper</CompassWrapper>
);
