import { Task } from '../../config/task-graph';
export declare function handleHashTasks(payload: {
    runnerOptions: any;
    tasks: Task[];
}): Promise<{
    response: string;
    description: string;
}>;
