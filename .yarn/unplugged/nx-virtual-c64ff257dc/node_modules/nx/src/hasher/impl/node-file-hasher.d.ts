import { FileHasher, FileHasherBase } from './file-hasher-base';
import ignore from 'ignore';
export declare class NodeFileHasher extends FileHasherBase implements FileHasher {
    ignoredGlobs: ReturnType<typeof ignore>;
    init(): Promise<void>;
    private allFilesInDir;
    hashFile(path: string): string;
    private hashContent;
}
export declare function nodeHashArray(input: string[]): string;
