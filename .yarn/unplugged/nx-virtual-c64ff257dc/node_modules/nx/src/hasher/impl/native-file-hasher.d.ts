import { FileHasher, FileHasherBase } from './file-hasher-base';
export declare class NativeFileHasher extends FileHasherBase implements FileHasher {
    static available(): boolean;
    init(): Promise<void>;
    hashFile(path: string): string;
}
export declare function nativeHashArray(input: string[]): string;
