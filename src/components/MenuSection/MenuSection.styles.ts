import menu from '../Menu/Menu.styles';

const section = {
  ...menu,
  borderRadius: 0,
};

const sectionTitle = {
  fontSize: 'sm',
  fontWeight: '3',
  color: 'text.secondary',
  height: '36px',
  ml: 'sm',
  justifyContent: 'center',
};

export default {
  section,
  sectionTitle,
};
