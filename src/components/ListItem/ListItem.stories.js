import React from 'react';
import ListItem from './ListItem';

export default {
  title: 'ListItem',
  component: ListItem,
};

export const Default = () => (
  <ListItem title="Single List Item" />
);
