const text = {
  display: 'inline',
};

const hiddenContainer = {
  visibility: 'hidden',
  position: 'absolute',
  p: 0,
  m: 0,
  border: 0,
  whiteSpace: 'normal',
  wordBreak: 'break-word',
};

export default {
  text,
  hiddenContainer,
};
