import { Meta } from '@storybook/addon-docs/blocks';
import * as ArrayFieldStories from './ArrayField.stories';
import AccessibilityTable from '../../utils/docUtils/AccessibilityTable';
import { KeyboardRows } from '../../utils/docUtils/accessibilityTableConstants';

<Meta of={ArrayFieldStories} isTemplate />

# ArrayField

The component displays array collections to provide useful functions and optimizations for arrays.

ArrayField should:
- Be used for dynamic-sized arrays/lists.
- Be used with common array/list manipulations.
- Be used to create an instance or collect information.

### Required Components

This component requires the appropriate field component ([TextField](./?path=/story/form-textfield--default) or [SelectField](./?path=/story/form-selectfield--default)) and the ArrayFieldDeleteButton.

The `label` and `helperText` props describe the array as a whole. `helperText` is array-level guidance, not helper text for an individual child field. ArrayField renders the helper and maximum-size message after the child rows and before the Add Field control.

The examples use compact rows without repeated visible child labels. Every child control still has an explicit accessible name, and every delete control has a name that describes its action. When supplying a custom `renderField` or `onComponentRender`, preserve an accessible name with `aria-label`, `aria-labelledby`, or a visible label even when omitting a repeated label for layout.

### Accessibility

<AccessibilityTable keyboardRows={[
  { key: 'Space or Enter', description: 'When focused, selects the item.' },
  KeyboardRows.tabNextFocusableComponent,
  KeyboardRows.shiftTabFocusPrevious,
]} screenReaderRows={[
  'The input uses the **`aria-invalid`** attribute to alert users that the values entered are not in the expected format.',
  'The field and buttons use the **`aria-label`** attribute to provide accessible names.',
]} />