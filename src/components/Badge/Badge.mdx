import { Meta } from '@storybook/addon-docs/blocks';
import * as BadgeStories from './Badge.stories';
import AccessibilityTable from '../../utils/docUtils/AccessibilityTable';
import { KeyboardRows } from '../../utils/docUtils/accessibilityTableConstants';

<Meta of={BadgeStories} isTemplate />

# Badge

Badges are compact Box components used as filtering cues or contextual support. They should be clearly labeled and should not be used as buttons. 

This component uses [props from Theme-UI](https://theme-ui.com/sx-prop).

### Help Hints

Pass a `helpHint` to attach contextual help text to a badge. The badge itself becomes the tooltip
trigger — no separate icon or button is rendered — and the hint opens on hover or keyboard focus.
Use `helpHintProps` to customize the tooltip's placement and appearance.

### Required Components

This component can be used independently, and does not require additional components.

### Accessibility

<AccessibilityTable keyboardRows={[
  KeyboardRows.spaceEnterSelectsComponentSimple,
  KeyboardRows.tabFocusesComponent,
]} keyboardDescription="Badge itself is not focusable. However, when components are added to it, these keys provide additional functionality." />
