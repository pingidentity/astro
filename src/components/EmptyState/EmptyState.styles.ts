const container = {
  flexDirection: 'column',
  alignItems: 'center',
  justifyContent: 'center',
  // Fill the parent (e.g. the ListView placeholder wrapper) so the content
  // centers vertically. Falls back to auto height when the parent has none.
  height: '100%',
  width: '100%',
  gap: 'sm',
  py: 'lg',
  textAlign: 'center',
};

export default {
  container,
};
