const container = {
  alignItems: 'center',
  minWidth: '100%',
  with: '100%',
  ml: 'auto',
  flexGrow: 1,
};
const iconWrapper = {
  ml: 'md',
};

const wrapper = {
  ml: 'auto',
  alignItems: 'center',
};

export default {
  container,
  iconWrapper,
  wrapper,
};
