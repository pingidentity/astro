const container = {
  backgroundColor: 'neutral.90',
  borderRadius: '3px',
  height: '7px',
};

const percentageBar = {
  ...container,
  backgroundColor: 'active',
};

export default {
  container,
  percentageBar,
};
