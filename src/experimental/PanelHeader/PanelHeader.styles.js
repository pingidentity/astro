const container = {
  bg: 'accent.99',
  borderBottom: '1px',
  borderBottomColor: 'accent.90',
  borderBottomStyle: 'solid',
  justifyContent: 'center',
  minHeight: 60,
};

const controls = {
  alignItems: 'center',
  alignSelf: 'center',
  flexShrink: 0,
  gap: 'sm',
  ml: 'auto',
  mr: 'md',
};

const data = {
  alignItems: 'center',
};

const wrapper = {
  display: 'flex',
  flex: '1 1 0px',
  ml: 'md',
};

const imageWrapper = {
  ml: 'sm',
};

export default {
  container,
  controls,
  data,
  imageWrapper,
  wrapper,
};
