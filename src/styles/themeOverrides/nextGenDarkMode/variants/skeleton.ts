const base = {
  backgroundColor: '#ffffff22',
};

const text = {
  ...base,
};

const circular = {
  ...base,
};

const rounded = {
  ...base,
};

const skeleton = {
  base,
  text,
  circular,
  rounded,
};

export default skeleton;
