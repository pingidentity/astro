const hoveredPreview = {
  wrapper: {
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    position: 'absolute',
  },
  shaded: {
    opacity: 0.3,
    backgroundColor: '#000',
    width: '100%',
    height: '100%',
    position: 'absolute',
    borderRadius: 2,
  },
};

export default {
  hoveredPreview,
};
