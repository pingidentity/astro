const container = {
  bg: 'background.base',
  border: 'none',
  minHeight: 72,
  maxHeight: 72,
};

const controls = {
  alignSelf: 'start',
  pt: 'xs',
};

const wrapper = {
  mr: 'md',
};

export default {
  container,
  controls,
  wrapper,
};
