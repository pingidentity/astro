const accordion = {
  '&:not(:last-of-type)': {
    borderBottom: '1px solid',
    borderBottomColor: 'border.base',
  },
  pb: 'lg',
};

const body = {
  py: 'sm',
};

const header = {
  p: 'sm',
};

export default {
  accordion,
  body,
  header,
};
